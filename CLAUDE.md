# 担当者別ダッシュボード

営業担当者ごとの案件状況を見るダッシュボード。Azure Static Web Apps でホストし、
データは Google スプレッドシートから Azure Functions 経由で取得する。

## 構成

```
/src/index.html    入口（ブロック→支店→担当者）
/src/weekly.html   日次・週次確認 メンバー用
/src/monthly.html  月次確認 メンバー用
/src/shared.js     共通処理。設定・列定義・集計ロジックはすべてここ
/src/style.css     共通スタイル
/api/GetSheetData/ スプレッドシートをCSVで取得して返すAzure Functions
```

ビルド工程は無い。素のHTML/CSS/JSをそのまま配信する。
フレームワーク（React、Vue、ビルドツール）は導入しない。

## データ

- 取得元：Googleスプレッドシート（共有設定は「リンクを知っている全員が閲覧可」）
- `発生案件` シート＝案件データ、`目標` シート＝月別目標
- API は gviz 経由でシート名を指定して取得する。`export?format=csv` は先頭シートしか
  取れないので使わない
- **シートIDは `api/GetSheetData/index.js` にのみ置く。フロントのJSに書かない**
- 読み込めるシートは `ALLOWED_SHEETS` で限定する

## 守るルール

### 列の扱い

- 列名の定義は `shared.js` の `COL` と `TCOL` に集約する。HTML側に列名を直接書かない
- `COL` の値は配列で別名を並べられる。列名が変わったらここに足す
- ヘッダーは全角・半角カッコやスペースのゆれを NFKC 正規化で吸収している。この仕組みを外さない
- 列が無い場合は落とさず、「未入力」として表示を続ける

### 集計の定義

- **完工の判定は `完成日(予定)`**（`CONFIG.completionBasis = 'plan'`）。
  完成日(予定/実績)が**選択中の年に入っていれば**、契約日(実績)の有無・年やいつ契約したかは問わず、
  今日より前でも後でも実績として集計する（`d.isDoneInYear`）。今日時点で完工しているかどうかの判定
  （`d.isCompleted`、進行中の案件数やファネル表示に使う）とは別物なので、混同しないこと
- **契約売上**＝契約日(実績)がその年
- **契約時粗利**＝契約金額（税込）− 予算。**完工粗利**＝最終粗利（無ければ契約時粗利）
- **毀損**＝完工粗利率 − 契約時粗利率（pt）。予算が空欄の案件は集計から外す
- **発生コホート**＝反響日（または発生年）がその年。
  粗利＝発生数 × 成約率 × 平均単価 × 粗利率 の恒等式を崩さない
- **見込ランク**＝「見込」列の S/A/B を優先。空欄なら阻害要因の残数（0→S、1〜2→A、3以上→B）（`d.rankEff`）
- **見込み金額**＝契約前の案件の金額 × ランク別の反映率（`CONFIG.forecastWeights`：S=90%・A=70%・B=50%、ランク無しは0%）を合算
- `CONFIG.minYear`（現在2026）より前の案件は読み込み時に捨てる。
  支店・担当者・年のプルダウンにも出さない

### 目標

- `目標` シートの `ブロック名／支店名／担当者／年度／月／売上／粗利` を使う
- 担当者行と支店合計行が混在しても二重計上しないよう、細かいほうの行を優先する
- 金額の単位は自動判定（10万以上なら円、未満なら万円）

### 画面

- 絞り込みは ブロック→支店→担当者 の順にカスケードする。URLパラメータと連動させる
- 表示件数が多い表は上限を設けて「ほかN件」と出す。全件描画しない
- 金額は万円表示。`fmtMan` などの既存フォーマッタを使う
- 数字が出せない場合は 0 ではなく「―」を出す

## 変更するときの手順

1. `main` に直接pushしない。ブランチを切ってPRを出す（PRでステージング環境が作られる）
2. 集計ロジックを変えたら、その定義をこのファイルにも反映する
3. 新しい列を使う場合は `COL` に追加し、その列が無いときの挙動も書く

## 未実装（シートに列が無いため）

列を足せば自動で表示が切り替わる作りにしてある。勝手にダミーデータで埋めない。

| 機能 | 必要な列 |
|---|---|
| 阻害要因 | `阻害_価格` `阻害_提案` `阻害_担当者` `阻害_会社` `阻害_時期` `阻害_決裁者` `阻害_競合` |
| 次にやること | `次にやること` |
| 着工前と工事中の区別 | `着工日(実績)`（現在は完成予定1か月以内を工事中とみなす暫定ルール） |
| 毀損の要因分類 | `毀損要因` |
| アンケート | 別シート `アンケート`（システムIDで紐付け） |

## デバッグ

`?debug=1` または画面右上の「データ診断」で、実際のヘッダー名・入力件数・
日付として読めた件数・読めなかった値の例が出る。
「特定の列だけ反映されない」ときはまずここを見る。
